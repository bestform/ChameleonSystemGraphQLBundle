# This Schema is auto generated by chameleon. To update it, please use the console command chameleon_system:graph_ql:create_schema
type Query {
{% for element in elements %}
    {% for field in element.queryFieldsUnique %}
{{ element.name }}({{ field.name }}: String!): {{ element.type }}
    {% endfor %}
{{ element.name_plural }}{% if element.queryFields %}({% for field in element.queryFields %}{{ field.name }}: String{% if not loop.last %}, {% endif %}{% endfor %}){% endif %}: [{{ element.type }}]
{% endfor %}
}

{% for element in elements %}
type {{ element.type }} {
{% for field in element.displayFields %}
    {{ field.name }}: {{ field.type }}
{% endfor %}
}

{% endfor %}